def log_message(user_id, phone_number, username, input_msg, output_msg, intent, channel):
    # Aquí se guarda en la base de datos relacional los mensajes
    # Estructura: user_id, phone_number, username, input_msg, output_msg, timestamps, intent, channel
    pass
